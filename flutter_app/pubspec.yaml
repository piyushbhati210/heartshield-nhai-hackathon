name: heartshield_app
description: HeartShield - Offline Face Recognition + Liveness Detection for NHAI Hackathon 7.0
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.19.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5+9
  sqflite: ^2.3.0
  path: ^1.8.3
  permission_handler: ^11.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
